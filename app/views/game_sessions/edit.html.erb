<p id="edit-booked">Are you sure you want to book this <%= @game_session.game.name %> session with <%= @users.find(@game_session.creator_id).username %>?</p>

<div class="container" style="margin-top: 20vh;">

  <%= simple_form_for @game_session do |f| %>
    <%= f.hidden_field :booked, value: true %>
    <%= f.submit "Validate", class: "btn btn-flat", id: "validate" %>
  <% end %>

  <%= link_to 'You go back one step!', game_session_path(@game_session), class: "btn btn-flat", id: "backback" %>

</div>


